/**
 * openclaw-chatgpt-ingestor entry point.
 *
 * This package is server-only. Run the server with:
 *   npm run server   (dev, via tsx)
 *   npm start        (production, after build)
 *
 * See README.md for full documentation.
 */

// Delegate to the server — this file exists for completeness / tooling compatibility.
import './server.js';
